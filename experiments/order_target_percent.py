"""
This experiment attempts to recreate Quantopian's order_target_percent function.
"""

from ibapi.wrapper import EWrapper, Contract
from ibapi.client import EClient
from threading import Thread
import queue

# Settings
CURRENCY = "USD"


def orderTargetPercent(contract, percent, style=None):
    """ Imitates Quantopian's order_target_percent.
    """

if __name__ == '__main__':
    app = TestApp("127.0.0.1", 7497, 10)

    try:
        contract = 
    finally:
        app.disconnect()